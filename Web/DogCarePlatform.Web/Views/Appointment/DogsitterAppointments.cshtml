@model List<DogCarePlatform.Web.ViewModels.Appointment.AppointmentViewModel>
@using DogCarePlatform.Common
@using System.Globalization
@{
    ViewData["Title"] = GlobalConstants.ListDogsitterAppointments;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>Вашите уговорки</h3>
<div>
    @foreach (var appointment in Model)
    {
        <div class="row">
            <div class="col s5 m5 l6">
                @*<div class="card horizontal">
                        <div class="card-image">
                            <img src="@appointment.Owner.ImageUrl">
                        </div>
                        <div class="card-stacked">
                            <div class="card-content">
                                <p>@appointment.Date.ToString("dddd, dd MMMM yyyy", new CultureInfo("bg-BG")) от <b class="amber-text text-darken-1">@appointment.StartTime.ToString("HH:mm") до @appointment.EndTime.ToString("HH:mm")</b></p>
                            </div>
                            <div class="card-action right">
                                <a href="#">This is a link</a>
                            </div>
                        </div>
                    </div>*@
                <ul class="collapsible">
                    <li>               
                        <div class="collapsible-header">
                            <div><h5>@appointment.Owner.FirstName @appointment.Owner.LastName</h5></div>
                            <img style="padding: 10px 10px 10px 10px;" class="circle responsive-img valign-wrapper" src="@appointment.Owner.ImageUrl" />
                            <h6 class="valign-wrapper">
                                @appointment.Owner.Address
                            </h6>
                            <h6 class="valign-wrapper">
                                @appointment.Date.ToString("dddd, dd MMMM yyyy", new CultureInfo("bg-BG")) от&nbsp
                                <b class="amber-text text-darken-1"> @appointment.StartTime.ToString("HH:mm")</b>&nbspдо&nbsp
                                <b class="amber-text text-darken-1"> @appointment.EndTime.ToString("HH:mm")</b>
                            </h6>
                        </div>
                        <div class="collapsible-body">
                            <span>Lorem ipsum dolor sit amet.</span>
                            <hr />
                            <div></div>
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    }
</div>

@section Scripts{
    <script>
        $(document).ready(function () {
            $('.collapsible').collapsible();
        });
    </script>
}